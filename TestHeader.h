#ifndef TESTHEADER // Header Security 'Lock'
#define TESTHEADER

#include "FileHeader.h" // Include main header file to allocate all library functions.

// The two test cases to determine if the information placed in the sort functions did indeed
// sort in the correct manner.
void testSortAscend(void);
void testSortDescend(void);

#endif // End of Security 'Lock'